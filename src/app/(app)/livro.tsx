/**
 * Rota do LIVRO — a leitura do diário no celular.
 *
 * Fora das abas de propósito: as cinco abas são lugares onde se REGISTRA, e o
 * livro é leitura. Ele se alcança pelo cartão da Início, que é de onde a
 * pessoa vem depois de fechar o dia.
 */
export { default } from '@/features/story/DiaryBookScreen';
