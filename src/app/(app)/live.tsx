/**
 * Rota da configuração das LIVE ACTIVITIES (o dia e o foco na tela de
 * bloqueio). Fora das abas: chega-se por Ajustes, onde mora o que é do aparelho.
 */
export { default } from '@/features/live/LiveSettingsScreen';
