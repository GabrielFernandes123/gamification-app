/**
 * Rota do SONO.
 *
 * Fora das abas, como o livro: as cinco abas são lugares onde se registra, e o
 * sono chega sozinho pelo HealthKit. Alcança-se pelo lançador de módulos da
 * Início — que é de onde `sleep` apontava para o web por não ter tela aqui.
 */
export { default } from '@/features/health/SleepScreen';
