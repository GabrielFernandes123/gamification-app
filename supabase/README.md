# Supabase

As migrations do banco vivem em `../../gamificacao-api/migrations`.

Este diretorio permanece no app apenas para configuracao local do Supabase CLI
(`config.toml`, `.temp`, snippets etc.). Nao crie novas migrations de dominio
aqui; a API e a fonte de verdade do schema.
